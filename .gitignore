# laptop-only / runtime
.venv/
__pycache__/
*.pyc
captures/
.tmp/
.swapfile

# model weights - large; get them with models/fetch_models.sh
models/*.gguf
# Torq NPU runtime (shared/torq_gemma): compiled model + runtime venv + wheels are large / built locally
models/npu_gemma/
.venv-npu/
torq_wheelhouse/
